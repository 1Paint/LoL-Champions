<p class="note">A missing set of values is a set of missing information such as base damages or scaling numbers.<br>
  For example, there are 2 sets of missing values for "deals {{ eX }} + {{ fX }} damage" where eX and fX are missing.</p><br><br>

<% Champion.order(:champ_name_id).each do |champ| %>
  <% buttons = ['Q', 'W', 'E', 'R'] %>
  <% missing_data = [champ[:missing_q],
                     champ[:missing_w],
                     champ[:missing_e],
                     champ[:missing_r]] %>
  <% if missing_data.count(nil) != 4 or champ[:bad_passive] == true %>
    <h3 class="missingdata"><a href=<%= "/champions/#{champ[:champ_name_id]}" %>><%= champ[:name] %></a></h3>
    
    <% if champ[:bad_passive] == true %>
      <p class="missingdata"><%= "Passive description is missing." %></p>
    <% end %>
    
    <% for i in 0..3 %>
      <% if missing_data[i] != nil %>
        <p class="missingdata"><%= "#{buttons[i]} is missing #{missing_data[i]} set(s) of values." %></p>
      <% end %>
    <% end %>
    <br>
    
  <% end %>
<% end %>

<script>
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

  ga('create', 'UA-77166231-1', 'auto');
  ga('send', 'pageview');

</script>