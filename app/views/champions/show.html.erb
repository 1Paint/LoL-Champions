<%= render 'champ_info' %>

<%= render 'passive' %>

<% @buttons = ["q", "w", "e", "r"] %>

<% @buttons.each do |button| %>
  <% @spell_button = button %>
  <%= render 'spell' %>
<% end %>

<%= render 'lore' %>

<%= render 'champ_splash' %>